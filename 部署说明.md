# 甲友乐 JYL 项目部署指南

该指南将指导您如何将“甲友乐”项目从开发环境部署到生产服务器（Linux/Ubuntu/CentOS）。

## v1.8.13 安全与数据正确性修复：升级必读（2026-09-05）

本次更新包含 **需要调整部署配置** 的变更，升级前请通读本节。

### 必须补充的环境变量

在 `server/.env`（或 Docker 的 `docker/.env_docker`）中新增以下三项：

```ini
# 是否信任反向代理写入的 X-Forwarded-* 头
# 部署在 Nginx / 网关后面：设为 true，否则拿到的都是代理 IP
# 直接把端口暴露给公网：必须保持 false，否则任何客户端都能用
# X-Forwarded-For 伪造来源 IP，绕过登录/验证码限流并污染操作日志
TRUST_PROXY=true
# 服务端前面的可信代理层数，只认转发头最后 N 跳
TRUST_PROXY_HOPS=1
# App 安装包 / 热更新包的公开访问域名，例如 https://jyl.example.com
# 不配置时服务端只返回相对路径（绝不会再用 Host 头拼绝对地址）
APP_PUBLIC_BASE_URL=https://your-domain.com
```

`TRUST_PROXY` 之前是硬编码开启的。这次改成默认关闭，**使用 Nginx 反向代理的部署必须显式设为 `true`**，否则限流与操作日志里记录的会是代理容器的 IP，而不是真实客户端 IP。对应的 Nginx 配置需要包含：

```nginx
proxy_set_header X-Real-IP        $remote_addr;
proxy_set_header X-Forwarded-For  $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host $host;
```

### 行为变更（需要注意）

- **`/api/health` 在数据库断连时返回 503**（此前恒返回 200）。监控平台如果按“状态码 200 即健康”配置，现在才能真正发现数据库故障；如果原先有依赖 200 的自动化脚本，请同步调整。
- **服务改为等数据库就绪后才开始监听端口**。启动时间会比以前长（数据库慢启动时最多多等几十秒），但启动期间不再对外返回一片失败。Docker 已配置 `HEALTHCHECK`，`start-period` 为 40 秒。
- **首页、洞察、复查建议、监测方案默认只统计本人数据**。此前这些接口会把家庭成员的化验记录一起算进来，用户看到的异常数、趋势和复查日期会因此变化——这是修正而非回退。需要跨成员汇总时传 `memberId=all`，指定成员传 `memberId=<成员ID>`。
- **服药依从率不再可能超过 100%**。停用计划或修改服药规则后，历史依从率会按正确口径重算，数值可能低于升级前显示的值。
- **新增 `POST /api/auth/account/delete`**（账号注销）。此前设置页的“注销账号”只是本地提示、服务端不做任何删除；现在会真正删库，且要求手输“确认注销”并二次校验密码或验证码。

### 必跑数据库自检

```bash
cd server
npm run migrate
npm run migrate:check    # 已改为按模型定义全量比对，会报出所有缺失字段
```

迁移自检从“手工维护字段清单”改成了“按模型定义全量比对”。**如果你的生产库是早期用 `sequelize.sync` 建的，这次自检可能报出此前从未被发现的缺列**，请按提示补迁移后再上线。

### 密钥外泄修复（重要）

新增了根目录 `.dockerignore`，阻断 `server/.env`、`docker/.env_docker`、`certs/` 进入构建上下文。此前 `docker build` 会把这些文件里的 `JWT_SECRET`、数据库密码、腾讯云与 SMTP 凭据一起固化进镜像层。

**已经构建并推送过旧镜像的部署，必须视这些凭据为已泄露**：

```bash
# 1. 轮换全部密钥（JWT_SECRET 轮换会让所有用户重新登录，属预期行为）
vim docker/.env_docker

# 2. 删除本地旧镜像，重新构建
sudo docker compose --env-file .env_docker down
sudo docker image rm jyl-app 2>/dev/null || true
sudo docker compose --env-file .env_docker build --no-cache jyl-app
sudo docker compose --env-file .env_docker up -d

# 3. 如果推送过镜像仓库，删除远端所有历史 tag
```

`npm run release:check` 现在会在 `.dockerignore` 缺失或未排除密钥文件时直接失败。

### 依赖升级

服务端生产依赖升级以消除高危公告（Koa Host 头注入、Sequelize JSON cast 注入、mysql2 认证降级泄露明文凭据等）。Docker 基础镜像同时从 `node:20-alpine` 升到 `node:24-alpine`（Node 20 已于 2026-04-30 结束维护）。非 Docker 部署请确认服务器 Node 版本 ≥ 22：

```bash
cd server && npm ci && npm run verify   # lint + test + 生产依赖高危审计
```

## v1.8.10 发布与安卓热更新注意事项（2026-06-20）

本次更新聚焦指标显示修复和 Android 热更新发布治理：记录详情页会显示本人或家庭成员自定义参考范围，趋势页相关记录单位字号已缩小，应用版本同步递增到 `1.8.10 / 190`。

### 本次发布内容

- 记录详情页优先显示记录所属本人或家庭成员的自定义参考范围。
- 详情页指标颜色、上下箭头和建议文案统一使用同一套有效参考范围。
- 指标单位优先级为自定义单位、报告原始单位、系统默认单位。
- 趋势页相关记录单位字号缩小，并保留自定义单位高亮。

### Android 自动更新设置

- 本次不涉及原生权限、原生模块、插件、包名、证书、图标、启动图或生产 API 地址，可通过 WGT 热更新发布，无需重新打 APK。
- `client/src/manifest.json` 已递增到 `versionName: 1.8.10`、`versionCode: 190`。
- 发布后 `storage/app-updates/manifest.json` 必须指向 `/storage/app-updates/jyl-1.8.10-190.wgt`。
- 后端更新检查会忽略小于或等于当前 `versionCode` 的包；已装 `1.8.9 / 189` 的用户只有在发布 `1.8.10 / 190` 后才会收到本次修复。
- 发布后请执行 `npm run release:check`，并用真机验证可发现、下载、安装并重启。

## v1.7.0 升级与发布注意事项（2026-05-14）

本次升级包含智能复查、指标异常分析、家庭成员增强档案、指标录入页 OCR 识别与复核闭环、分享管理、数据洞察和 H5 缓存治理能力。生产环境发布前请先备份数据库，再执行后端迁移。

### 必跑数据库迁移

在 `server` 目录执行：

```bash
npm run migrate
```

本次新增迁移：
- `20260514_add_share_links.js`：创建 `ShareLinks` 表，支持分享链接哈希 token、有效期、撤销、访问次数、最近访问时间、访问 IP 和分享选项。
- `20260514_add_expansion_fields.js`：补充家庭成员参考范围、复查周期、分享字段脱敏配置等扩展字段。

生产环境建议显式设置 `DB_SYNC_ALTER=false`，避免自动改表；所有结构升级统一通过迁移脚本完成。Docker 部署时请在容器内执行同样的 `npm run migrate`。

### 新增能力入口

- `pages/insight/dashboard`：数据洞察页，展示月度趋势、异常次数、服药依从性、复查完成率和智能复查建议。
- 指标录入页 OCR 流程：OCR 识别结果在录入页内修正和应用；独立 `pages/ocr/review` 入口已在 `1.8.9` 收敛，不再作为发布入口。
- `pages/share/manage`：分享管理页，支持创建、撤销、有效期、脱敏字段和访问记录。
- `pages/my/family-profile`：家庭成员增强档案，支持成员独立病种、参考范围和复查周期。

### 运维与安全提醒

- 上传接口现在会校验真实图片文件签名，仅允许合法 jpg/png/webp。
- 分享链接使用不透明 token，数据库只保存哈希值；过期或撤销链接会由清理任务处理。
- 日志和 500 错误响应已做敏感信息保护，生产环境不要关闭该保护。
- `/api/tip/seed` 已限制为管理员调用，请确认生产账号权限配置正确。
- 新增 `/api/health` 接口，用于监控服务运行状态和数据库连接情况，可集成至 UptimeRobot 或类似监控平台。

## 1. 部署架构说明

*   **前端**：uni-app (Vue 3) 构建的 H5产物。
*   **后端**：Node.js (Koa 2) 服务，负责提供 API 接口并直接托管前端静态文件。
*   **数据库**：MySQL 5.7+。
*   **进程管理**：PM2 (确保后端服务挂掉后自动重启)。
*   **反向代理**：Nginx (负责 SSL 证书配置及请求转发)。

---

## 2. 准备工作

在服务器上安装以下必要软件：
- **Node.js**: v16 或更高版本。
- **MySQL**: 5.7+。
- **PM2**: `npm install -g pm2`。
- **Git**: 用于拉取代码。

---

## 3. 部署步骤

### 第一步：获取代码并安装依赖

```bash
# 克隆代码
git clone <your-repository-url>
cd jyl

# 安装后端依赖
cd server
npm install

# 安装前端依赖
cd ../client
npm install
```

### 第二步：配置环境并准备前端产物

1.  **前端构建**：
    在 `client` 目录下执行构建命令，生成 H5 静态资源：
    ```bash
    npm run build:h5
    ```
    构建产物将位于 `client/dist/build/h5`。

2.  **后端环境配置**：
    进入 `server` 目录，将 `.env.example` 复制为 `.env` 并根据实际情况修改：
    ```bash
    cp .env.example .env
    # 使用 vim 或 nano 编辑配置数据库连接、JWT 密钥、腾讯云 OCR 密钥等
    vim .env
    ```

3.  **数据库初始化**：
    确保服务器上已创建对应的数据库名，后端通过 `DbService.init()` 在启动时会自动同步表结构并初始化种子数据（百科条目、贴士等）。

4.  **数据库迁移（生产环境）**：
    生产环境已有数据时，不建议使用自动 `sync alter`。请使用迁移脚本进行结构升级：
    ```bash
    cd server
    npm install
    # 确保已设置环境变量：DB_HOST / DB_PORT / DB_NAME / DB_USER / DB_PASS
    npm run migrate
    ```
    如果服务器设置了 `NODE_ENV=production`，请使用 `npm install --include=dev` 以安装 `sequelize-cli`，
    或改用 `npx sequelize-cli db:migrate`。
    如需回滚最近一次迁移：
    ```bash
    npm run migrate:undo
    ```
    迁移说明文档位于：`server/migrations/README.md`。

5.  **Docker 部署执行迁移**：
    若使用 Docker 部署，迁移需要在容器内执行：
    ```bash
    cd docker
    # 确保容器已启动
    sudo docker compose --env-file .env_docker exec jyl-app npm run migrate
    sudo docker compose --env-file .env_docker exec jyl-app npm run migrate:check
    sudo docker compose --env-file .env_docker restart jyl-app
    ```
    本次升级新增用药补签字段和检查记录页，迁移后请确认 `migrate:check` 通过，再重启应用容器。
    如需回滚最近一次迁移：
    ```bash
    cd docker
    sudo docker compose --env-file .env_docker exec jyl-app npm run migrate:undo
    ```
    旧版命令可将 `docker compose` 替换为 `docker-compose`。

    建议在 `.env_docker` 中显式设置 `NODE_ENV=production` 与 `DB_SYNC_ALTER=false`，避免生产环境自动改表。

### 第三步：使用 PM2 启动后端服务

在 `server` 目录下启动：
```bash
pm2 start index.js --name jyl-server
pm2 save
pm2 startup
```
*提示：后端会自动托管上一步生成的 `client/dist/build/h5` 中的静态文件，您无需单独配置 Nginx 托管前端。*

---

## 4. (可选) 配置 Nginx 与 SSL

通常建议在前端增加一层 Nginx 反向代理，以提供 HTTPS 支持：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/fullchain.pem;
    ssl_certificate_key /path/to/privkey.pem;

    location / {
        proxy_pass http://localhost:3000; # 转发到 Koa 后端
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

---

## 5. 存储与权限

确保后端进程对根目录下的 `storage` 文件夹有读写权限，以便能够保存 OCR 上传的化验单图片：
```bash
sudo chown -R $USER:$USER storage
chmod -R 755 storage
```

---

## 6. 常见问题排查

1.  **页面 404**：如果您直接在浏览器地址栏刷新 SPA 路由（如 `/pages/record/list`），后端已经配置了回退机制返回 `index.html`。
2.  **图片不显示**：检查 `server/.env` 中的环境变量，确保访问地址与后端端口或 Nginx 配置一致。
3.  **数据库连接错误**：请确保 MySQL 运行正常，且 `.env` 中的用户名、密码 and 端口填写正确。

---

## 7. 使用 Docker 一键部署 (推荐方案)

为了保持项目根目录整洁，所有 Docker 相关文件均存放在专用的 **`docker/`** 目录下。

### 7.1 全新部署 (Fresh Install)

1.  **准备环境**：
    确保服务器已安装 **Docker** 和 **Docker Compose**。

2.  **配置环境参数**：
    在 `docker/` 目录下，将 **`.env_docker.example`** 复制为 **`.env_docker`**：
    ```bash
    cd docker
    cp .env_docker.example .env_docker
    # 使用 vim 编辑业务配置 (如 JWT 密钥、OCR 的 SecretID/Key 等)
    vim .env_docker
    ```

3.  **构建并启动容器**：
    在 `docker/` 目录下执行：
    ```bash
    # 使用 --build 强制从源码重新编译前端和后端镜像
    sudo docker compose --env-file .env_docker up -d --build
    ```
    *注：首次启动可能需要几分钟时间来下载 MySQL 指向和安装 NPM 依赖。*

### 7.2 升级部署 (Update & Redeploy)

当您拉取了新的代码（例如路由变更、指标名称修正）后，请按以下步骤操作：

1.  **同步最新代码**：
    ```bash
    git pull origin main
    ```

2.  **一键重构并更新**：
    在 `docker/` 目录下执行，这将触发多阶段构建，**自动清理旧的 H5 缓存文件**：
    ```bash
    # 强制无缓存构建应用镜像
    sudo docker compose --env-file .env_docker build --no-cache jyl-app
    # 重新启动应用容器并应用新镜像
    sudo docker compose --env-file .env_docker up -d --no-deps jyl-app
    ```

3.  **验证版本**：
    访问您的域名，进入“我的”页面，检查底部的版本号（如 v1.6.7）是否已更新。

### 7.3 💡 生产环境避坑指南 (Cache)

*   **旧版 PWA 缓存清理**：当前版本已停止生成 PWA Service Worker。后端会对历史 `sw.js`、`registerSW.js` 和 `manifest.webmanifest` 返回清理响应；若仍看到旧页面，可在开发者模式的 `Application -> Storage` 点击 **Clear site data**，或按 `Ctrl+F5` 强制刷新。
*   **API 地址注入**：对于多端部署（如未来发布小程序），可以在构建时注入域名：
    ```bash
    # 仅在非同源部署时需要
    sudo docker compose build --build-arg VITE_API_BASE=https://api.yourdomain.com jyl-app
    ```

### Docker 方案优势

- **物理清理**：项目已配置 `emptyOutDir: true`，Docker 每次构建都会清空镜像内的 `dist/assets` 目录，绝对不会残留旧版本的 JS 代码碎片。
- **环境隔离**：Node.js 与 MySQL 版本完全独立，不受宿主机干扰。
- **持久化挂载**：`storage` 图片目录和 `mysql_data` 数据库卷已自动映射，重启后数据不丢失。
- **自动化反代**：Node.js 后端自动托管构建好的 H5 静态文件，无需额外配置 Nginx 映射静态路径。
